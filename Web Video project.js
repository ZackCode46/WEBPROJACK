// This is an empty JavaScript file.
// script.js

document.addEventListener("DOMContentLoaded", function() {
    // This is where you can add JavaScript interactions, like toggling sections, etc.

    // Example: Adding a simple alert when the video section is clicked
    const videoSection = document.querySelector('.video-section');
    videoSection.addEventListener('click', () => {
        alert("You clicked on the Video Section!");
    });

    // If you want to dynamically add content later, you can also do that here.
});
